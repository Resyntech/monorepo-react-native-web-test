import React from "react";
import { ScrollView } from "react-native";
import Home from "./src/screens";

const App = () => {
  return (
    <ScrollView>
      <Home />
    </ScrollView>
  );
};

export default App;
